
==1

const int trigPin = 8;
const int echoPin = 7;
int motorPin = 3;

long duration;
int distance;

void setup()
{
  pinMode(trigPin, OUTPUT);
  pinMode(echoPin, INPUT);
  pinMode(motorPin, OUTPUT);
  Serial.begin(9600);
}

void loop()
{
  digitalWrite(trigPin, LOW);
  delayMicroseconds(5);
  
  digitalWrite(trigPin, HIGH);
  delayMicroseconds(10);
  digitalWrite(trigPin, LOW);
  
  duration = pulseIn(echoPin, HIGH);
  distance = duration*0.034/2;
  
  Serial.print("Distance: ");
  Serial.println(distance);

  if (digitalRead(echoPin)==HIGH)
  {
    digitalWrite(motorPin, HIGH);
    delayMicroseconds(1000);
    digitalWrite(motorPin, LOW);
    delayMicroseconds(1000);
  }
}

==2

  int motorPin = digitalRead(distance);

  if (distance==100)
  {
    digitalWrite(motorPin, HIGH);
    delayMicroseconds(1000);
    digitalWrite(motorPin, LOW);
    delayMicroseconds(1000);
  }
  else if (distance==75)
  {
    digitalWrite(motorPin, HIGH);
    delayMicroseconds(750);
    digitalWrite(motorPin, LOW);
    delayMicroseconds(750);
  }
  else if (distance==50)
  {
    digitalWrite(motorPin, HIGH);
    delayMicroseconds(500);
    digitalWrite(motorPin, LOW);
    delayMicroseconds(500);
  }
  else if (distance==25)
  {
    digitalWrite(motorPin, HIGH);
    delayMicroseconds(250);
    digitalWrite(motorPin, LOW);
    delayMicroseconds(250);
  }
}

==3

if (echoPin==HIGH);
  {
     digitalWrite(motorPin, HIGH);
     delay(1000);
     digitalWrite(motorPin Dec EE, LOW);
     delay(1000);
   }
 }

==4

  if (digitalRead(distance)<=100)
  {
    digitalWrite(motorPin, HIGH);
    delayMicroseconds(1000);
    digitalWrite(motorPin, LOW);
    delayMicroseconds(1000);
  }
  else if (digitalRead(distance)<=75)
  {
    digitalWrite(motorPin, HIGH);
    delayMicroseconds(750);
    digitalWrite(motorPin, LOW)
    delayMicroseconds(750);
  }
  else if (digitalRead(distance)<=50)
  {
    digitalWrite(motorPin, HIGH);
    delayMicroseconds(500);
    digitalWrite(motorPin, LOW);
    delayMicroseconds(500);
  }
  else if (digitalRead(distance)<=25)
  {
    digitalWrite(motorPin, HIGH);
    delayMicroseconds(250);
    digitalWrite(motorPin, HIGH);
    delayMicroseconds(250);                         
  }
}

==5 (working)

  if (distance<=100)
  {
     digitalWrite(motorPin, HIGH);
   }
 }

==6

  if (distance<=100)
  {
    digitalWrite(motorPin, HIGH);
    delay(1000);
    digitalWrite(motorPin, LOW);
    delay(1000);

    delay(1000);
  }
}

==7 (vibration working)

  if (distance<=100)
  {
    vibrate();
  }
  else
  {
    stop();
  }
}

void vibrate()
{
  digitalWrite(motorPin, HIGH);
  delay(1000);
  digitalWrite(motorPin, LOW);
  delay(1000);
}

void stop()
{
  digitalWrite(motorPin, LOW);
}

==8 (vibration working full code with distance)

const int trigPin = 8;
const int echoPin = 7;
int motorPin = 3;

long duration;
int distance;

void setup()
{
  pinMode(trigPin, OUTPUT);
  pinMode(echoPin, INPUT);
  pinMode(motorPin, OUTPUT);
  Serial.begin(9600);
}

void loop()
{
  digitalWrite(trigPin, LOW);
  delayMicroseconds(5);
  digitalWrite(trigPin, HIGH);
  delayMicroseconds(10);
  
  digitalWrite(trigPin, LOW);
  duration = pulseIn(echoPin, HIGH);
  distance = duration*0.034/2;
  
  Serial.print("Distance: ");
  Serial.println(distance);
  
  if (distance<=100)
  {
    vibrate1(); 
  {
  if (distance<=75)
  {
    vibrate2();	
  }
  if (distance<=50)
  {
    vibrate3();
  }
  if (distance<=25)
  {
    vibrate4();
  }
  else
  {
    stop();  
  }
}
 
void vibrate1()
{
  digitalWrite(motorPin, HIGH);
  delay(1000);
  digitalWrite(motorPin, LOW);
  delay(1000);
}

void vibrate2()
{
  digitalWrite(motorPin, HIGH);
  delay(750);
  digitalWrite(motorPin, LOW);
  delay(750);
}

void vibrate3()
{
  digitalWrite(motorPin, HIGH);
  delay(500);
  digitalWrite(motorPin, LOW);
  delay(500);
}

void vibrate4()
{
  digitalWrite(motorPin, HIGH);
  delay(250);
  digitalWrite(motorPin, LOW);
  delay(250);
}

void stop()
{
  digitalWrite(motorPin, LOW);
}

==9 (integrated ring void)

const int trigPin = 8;
const int echoPin = 7;
int motorPin = 3;
int ringPin = 5;

long duration;
int distance;

void setup()
{
  pinMode(trigPin, OUTPUT);
  pinMode(echoPin, INPUT);
  pinMode(motorPin, OUTPUT);
  pinMode(ringPin, OUTPUT);
  Serial.begin(9600);
}

void loop()
{
  digitalWrite(trigPin, LOW);
  delayMicroseconds(5);
  digitalWrite(trigPin, HIGH);
  delayMicroseconds(10);
  
  digitalWrite(trigPin, LOW);
  duration = pulseIn(echoPin, HIGH);
  distance = duration*0.034/2;
  
  Serial.print("Distance: ");
  Serial.println(distance);
  
  if ((distance >= 75) && (distance <= 100))
  {
    vibrate1();
    ring1();
  }
  if ((distance >= 50) && (distance <= 75))
  {
    vibrate2();	
    ring2();
  }
  if ((distance >= 25) && (distance <= 50))
  {
    vibrate3();
    ring3();
  }
  if ((distance >= 4) && (distance <= 25))
  {
    vibrate4();
    ring4();
  }
  else
  {
    stop();  
  }
}

void vibrate1()
{
  digitalWrite(motorPin, HIGH);
  delay(1000);
  digitalWrite(motorPin, LOW);
  delay(1000);
}

void vibrate2()
{
  digitalWrite(motorPin, HIGH);
  delay(750);
  digitalWrite(motorPin, LOW);
  delay(750);
}

void vibrate3()
{
  digitalWrite(motorPin, HIGH);
  delay(500);
  digitalWrite(motorPin, LOW);
  delay(500);
}

void vibrate4()
{
  digitalWrite(motorPin, HIGH);
  delay(250);
  digitalWrite(motorPin, LOW);
  delay(250);
}

void ring1()
{
  tone(ringPin, 311);
  delay(1000);
  noTone(ringPin);
  delay(1000);
}

void ring2()
{
  tone(ringPin, 330);
  delay(750);
  noTone(ringPin);
  delay(750);
}

void ring3()
{
  tone(ringPin, 349);
  delay(500);
  noTone(ringPin);
  delay(500);
}

void ring4()
{
  tone(ringPin, 370);
  delay(250);
  noTone(ringPin);
  delay(250);
}

void stop()
{
  digitalWrite(motorPin, LOW);
  noTone(ringPin);
}

==10 (ring and vibrate voids into one)

const int trigPin = 8;
const int echoPin = 7;
int motorPin = 3;
int ringPin = 5;

long duration;
int distance;

void setup()
{
  pinMode(trigPin, OUTPUT);
  pinMode(echoPin, INPUT);
  pinMode(motorPin, OUTPUT);
  pinMode(ringPin, OUTPUT);
  Serial.begin(9600);
}

void loop()
{
  digitalWrite(trigPin, LOW);
  delayMicroseconds(5);
  digitalWrite(trigPin, HIGH);
  delayMicroseconds(10);
  
  digitalWrite(trigPin, LOW);
  duration = pulseIn(echoPin, HIGH);
  distance = duration*0.034/2;
  
  Serial.print("Distance: ");
  Serial.println(distance);
  
  if ((distance >= 75) && (distance <= 100))
  {
    ribrate1();
  {
  if ((distance >= 50) && (distance <= 75))
  {
    ribrate2();
  }
  if ((distance >= 25) && (distance <= 50))
  {
    ribrate3();
  }
  if ((distance >= 4) && (distance <= 25))
  {
    ribrate4();
  }
  else
  {
    stop();  
  }
}

void ribrate1()
{
  digitalWrite(motorPin, HIGH);
  delay(1000);
  digitalWrite(motorPin, LOW);
  delay(1000);
  
  tone(ringPin, 311);
  delay(1000);
  noTone(ringPin);
  delay(1000);
}

void ribrate2()
{
  digitalWrite(motorPin, HIGH);
  delay(750);
  digitalWrite(motorPin, LOW);
  delay(750);
  
  tone(ringPin, 330);
  delay(750);
  noTone(ringPin);
  delay(750);
}

void ribrate3()
{
  digitalWrite(motorPin, HIGH);
  delay(500);
  digitalWrite(motorPin, LOW);
  delay(500);
  
  tone(ringPin, 349);
  delay(500);
  noTone(ringPin);
  delay(500);
}

void ribrate4()
{
  digitalWrite(motorPin, HIGH);
  delay(250);
  digitalWrite(motorPin, LOW);
  delay(250);
  
  tone(ringPin, 370);
  delay(250);
  noTone(ringPin);
  delay(250);
}

void stop()
{
  digitalWrite(motorPin, LOW);
  noTone(ringPin);
}

==11: millis in void loop ifs (vibrate only)

const int trigPin = 8;
const int echoPin = 7;
int motorPin = 3;
int ringPin = 5;
int motorPin_state = LOW;

unsigned long prevTime_V1 = millis();
unsigned long prevTime_V2 = millis();
unsigned long prevTime_V3 = millis();
unsigned long prevTime_V4 = millis();

long interval_V1 = 1000;
long interval_V2 = 750;
long interval_V3 = 500;
long interval_V4 = 250;

long duration;
int distance;

void setup()
{
  pinMode(trigPin, OUTPUT);
  pinMode(echoPin, INPUT);
  pinMode(motorPin, OUTPUT);
  pinMode(ringPin, OUTPUT);
  Serial.begin(9600);
}

void loop()
{
  digitalWrite(trigPin, LOW);
  delayMicroseconds(5);
  digitalWrite(trigPin, HIGH);
  delayMicroseconds(10);
  
  digitalWrite(trigPin, LOW);
  duration = pulseIn(echoPin, HIGH);
  distance = duration*0.034/2;
  
  Serial.print("Distance: ");
  Serial.println(distance);

  unsigned long currentTime = millis();
  
  if ((distance >= 75) && (distance <= 100) && (currentTime - prevTime_V1 > interval_V1))
  {
    vibrate1();
    ring1();
    prevTime_V1 = currentTime;
  }
  if ((distance >= 50) && (distance <= 75) && (currentTime - prevTime_V2 > interval_V2))
  {
    vibrate2();
    ring2();
    prevTime_V2 = currentTime;
  }
  if ((distance >= 25) && (distance <= 50) && (currentTime - prevTime_V3 > interval_V3))
  {
    vibrate3();
    ring3();
    prevTime_V3 = currentTime;
  }
  if ((distance >= 4) && (distance <= 25) && (currentTime - prevTime_V4 > interval_V4))
  {
    vibrate4();
    ring4();
    prevTime_V4 = currentTime;
  }
  else
  {
    stop();  
  }
}

void vibrate1()
{
  digitalWrite(motorPin, HIGH);
  motorPin_state = !motorPin_state;
}

void vibrate2()
{
  digitalWrite(motorPin, HIGH);
  motorPin_state = !motorPin_state;
}

void vibrate3()
{
  digitalWrite(motorPin, HIGH);
  motorPin_state = !motorPin_state;
}

void vibrate4()
{
  digitalWrite(motorPin, HIGH);
  motorPin_state = !motorPin_state;
}

void ring1()
{
  tone(ringPin, 311);
  delay(1000);
  noTone(ringPin);
  delay(1000);
}

void ring2()
{
  tone(ringPin, 330);
  delay(750);
  noTone(ringPin);
  delay(750);
}

void ring3()
{
  tone(ringPin, 349);
  delay(500);
  noTone(ringPin);
  delay(500);
}

void ring4()
{
  tone(ringPin, 370);
  delay(250);
  noTone(ringPin);
  delay(250);
}

void stop()
{
  digitalWrite(motorPin, LOW);
  noTone(ringPin);
}

==12 millis in void loop ifs (vibrate and ring)

const int trigPin = 8;
const int echoPin = 7;
int motorPin = 3;
int ringPin = 5;
int motorPin_state = LOW;
int ringPin_state = LOW;

unsigned long prevTime_V1 = millis();
unsigned long prevTime_V2 = millis();
unsigned long prevTime_V3 = millis();
unsigned long prevTime_V4 = millis();

long interval_V1 = 1000;
long interval_V2 = 750;
long interval_V3 = 500;
long interval_V4 = 250;

long duration;
int distance;

void setup()
{
  pinMode(trigPin, OUTPUT);
  pinMode(echoPin, INPUT);
  pinMode(motorPin, OUTPUT);
  pinMode(ringPin, OUTPUT);
  Serial.begin(9600);
}

void loop()
{
  digitalWrite(trigPin, LOW);
  delayMicroseconds(5);
  digitalWrite(trigPin, HIGH);
  delayMicroseconds(10);
  
  digitalWrite(trigPin, LOW);
  duration = pulseIn(echoPin, HIGH);
  distance = duration*0.034/2;
  
  Serial.print("Distance: ");
  Serial.println(distance);

  unsigned long currentTime = millis();
  
  if ((distance >= 75) && (distance <= 100) && (currentTime - prevTime_V1 > interval_V1))
  {
    vibrate1();
    ring1();
    prevTime_V1 = currentTime;
  }
  if ((distance >= 50) && (distance <= 75) && (currentTime - prevTime_V2 > interval_V2))
  {
    vibrate2();
    ring2();
    prevTime_V2 = currentTime;
  }
  if ((distance >= 25) && (distance <= 50) && (currentTime - prevTime_V3 > interval_V3))
  {
    vibrate3();
    ring3();
    prevTime_V3 = currentTime;
  }
  if ((distance >= 4) && (distance <= 25) && (currentTime - prevTime_V4 > interval_V4))
  {
    vibrate4();
    ring4();
    prevTime_V4 = currentTime;
  }
  else
  {
    stop();  
  }
}

void vibrate1()
{
  digitalWrite(motorPin, HIGH);
  motorPin_state = !motorPin_state;
}

void vibrate2()
{
  digitalWrite(motorPin, HIGH);
  motorPin_state = !motorPin_state;
}

void vibrate3()
{
  digitalWrite(motorPin, HIGH);
  motorPin_state = !motorPin_state;
}

void vibrate4()
{
  digitalWrite(motorPin, HIGH);
  motorPin_state = !motorPin_state;
}

void ring1()
{
  tone(ringPin, 311);
  ringPin_state = !ringPin_state;
}

void ring2()
{
  tone(ringPin, 330);
  ringPin_state = !ringPin_state;
}

void ring3()
{
  tone(ringPin, 349);
  ringPin_state = !ringPin_state;
}

void ring4()
{
  tone(ringPin, 370);
  ringPin_state = !ringPin_state;
}

void stop()
{
  digitalWrite(motorPin, LOW);
  noTone(ringPin);
}

==13: millis in vibrate voids (vibrate only)

const int trigPin = 8;
const int echoPin = 7;
int motorPin = 3;
int ringPin = 5;
int motorPin_state = LOW;

unsigned long prevTime_V1 = millis();
unsigned long prevTime_V2 = millis();
unsigned long prevTime_V3 = millis();
unsigned long prevTime_V4 = millis();

long interval_V1 = 1000;
long interval_V2 = 750;
long interval_V3 = 500;
long interval_V4 = 250;

long duration;
int distance;

void setup()
{
  pinMode(trigPin, OUTPUT);
  pinMode(echoPin, INPUT);
  pinMode(motorPin, OUTPUT);
  pinMode(ringPin, OUTPUT);
  Serial.begin(9600);
}

void loop()
{
  digitalWrite(trigPin, LOW);
  delayMicroseconds(5);
  digitalWrite(trigPin, HIGH);
  delayMicroseconds(10);
  
  digitalWrite(trigPin, LOW);
  duration = pulseIn(echoPin, HIGH);
  distance = duration*0.034/2;
  
  Serial.print("Distance: ");
  Serial.println(distance);
  
  if ((distance >= 75) && (distance <= 100))
  {
    vibrate1();
    ring1();
  }
  if ((distance >= 50) && (distance <= 75))
  {
    vibrate2();	
    ring2();
  }
  if ((distance >= 25) && (distance <= 50))
  {
    vibrate3();
    ring3();
  }
  if ((distance >= 4) && (distance <= 25))
  {
    vibrate4();
    ring4();
  }
  else
  {
    stop();  
  }
}

void vibrate1()
{
  unsigned long currentTime = millis();

  if (currentTime - prevTime_V1 > interval_V1)
  {
    digitalWrite(motorPin, HIGH);
    motorPin_state = !motorPin_state;
    prevTime_V1 = currentTime;
  }
}

void vibrate2()
{
  unsigned long currentTime = millis();

  if (currentTime - prevTime_V2 > interval_V2)
  {
    digitalWrite(motorPin, HIGH);
    motorPin_state = !motorPin_state;
    prevTime_V2 = currentTime;
  }
}

void vibrate3()
{
  unsigned long currentTime = millis();

  if (currentTime - prevTime_V3 > interval_V3)
  {
    digitalWrite(motorPin, HIGH);
    motorPin_state = !motorPin_state;
    prevTime_V3 = currentTime;
  }
}

void vibrate4()
{
  unsigned long currentTime = millis();

  if (currentTime - prevTime_V4 > interval_V4)
  {
    digitalWrite(motorPin, HIGH);
    motorPin_state = !motorPin_state;
    prevTime_V4 = currentTime;
  }
}

void ring1()
{
  tone(ringPin, 311);
  delay(1000);
  noTone(ringPin);
  delay(1000);
}

void ring2()
{
  tone(ringPin, 330);
  delay(750);
  noTone(ringPin);
  delay(750);
}

void ring3()
{
  tone(ringPin, 349);
  delay(500);
  noTone(ringPin);
  delay(500);
}

void ring4()
{
  tone(ringPin, 370);
  delay(250);
  noTone(ringPin);
  delay(250);
}

void stop()
{
  digitalWrite(motorPin, LOW);
  noTone(ringPin);
}

==14: millis in vibrate and ring voids

const int trigPin = 8;
const int echoPin = 7;
int motorPin = 3;
int ringPin = 5;
motorPin_state = LOW;
ringPin_state = LOW;

unsigned long prevTime_V1 = millis();
unsigned long prevTime_V2 = millis();
unsigned long prevTime_V3 = millis();
unsigned long prevTime_V4 = millis();

long interval_V1 = 1000;
long interval_V2 = 750;
long interval_V3 = 500;
long interval_V4 = 250;

long duration;
int distance;

void setup()
{
  pinMode(trigPin, OUTPUT);
  pinMode(echoPin, INPUT);
  pinMode(motorPin, OUTPUT);
  pinMode(ringPin, OUTPUT);
  Serial.begin(9600);
}

void loop()
{
  digitalWrite(trigPin, LOW);
  delayMicroseconds(5);
  digitalWrite(trigPin, HIGH);
  delayMicroseconds(10);
  
  digitalWrite(trigPin, LOW);
  duration = pulseIn(echoPin, HIGH);
  distance = duration*0.034/2;
  
  Serial.print("Distance: ");
  Serial.println(distance);
  
  if ((distance >= 75) && (distance <= 100))
  {
    vibrate1();
    ring1();
  }
  if ((distance >= 50) && (distance <= 75))
  {
    vibrate2();	
    ring2();
  }
  if ((distance >= 25) && (distance <= 50))
  {
    vibrate3();
    ring3();
  }
  if ((distance >= 4) && (distance <= 25))
  {
    vibrate4();
    ring4();
  }
  else
  {
    stop();  
  }
}

void vibrate1()
{
  unsigned long currentTime = millis();

  if (currentTime - prevTime_V1 > interval_V1)
  {
    digitalWrite(motorPin, HIGH);
    motorPin_state = !motorPin_state;
    prevTime_V1 = currentTime;
  }
}

void vibrate2()
{
  unsigned long currentTime = millis();

  if (currentTime - prevTime_V2 > interval_V2)
  {
    digitalWrite(motorPin, HIGH);
    motorPin_state = !motorPin_state;
    prevTime_V2 = currentTime;
  }
}

void vibrate3()
{
  unsigned long currentTime = millis();

  if (currentTime - prevTime_V3 > interval_V3)
  {
    digitalWrite(motorPin, HIGH);
    motorPin_state = !motorPin_state;
    prevTime_V3 = currentTime;
  }
}

void vibrate4()
{
  unsigned long currentTime = millis();

  if (currentTime - prevTime_V4 > interval_V4)
  {
    digitalWrite(motorPin, HIGH);
    motorPin_state = !motorPin_state;
    prevTime_V4 = currentTime;
  }
}

void ring1()
{
  unsigned long currentTime = millis();

  if (currentTime - prevTime_V1 > interval_V1)
  {
    tone(ringPin, 311);
    ringPin_state = !ringPin_state;
    prevTime_V1 = currentTime;
}

void ring2()
{
  unsigned long currentTime = millis();

  if (currentTime - prevTime_V2 > interval_V2)
  {
    tone(ringPin, 330);
    ringPin_state = !ringPin_state;
    prevTime_V2 = currentTime;
}

void ring3()
{
  unsigned long currentTime = millis();

  if (currentTime - prevTime_V3 > interval_V3)
  {
    tone(ringPin, 349);
    ringPin_state = !ringPin_state;
    prevTime_V3 = currentTime;
}

void ring4()
{
  unsigned long currentTime = millis();

  if (currentTime - prevTime_V4 > interval_V4)
  {
    tone(ringPin, 370);
    ringPin_state = !ringPin_state;
    prevTime_V4 = currentTime;
}

void stop()
{
  digitalWrite(motorPin, LOW);
  noTone(ringPin);
}



  


